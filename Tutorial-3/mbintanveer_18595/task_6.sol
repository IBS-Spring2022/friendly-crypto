//Consider the file visibility.sol
//● Change the visibility specifier of f() so that it can be called from derived contracts as
//well. Do not set it as being public.
//● Create a new contract that derives from A and call f() from the new contract.